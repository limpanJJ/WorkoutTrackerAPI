# Copilot Instructions

## Project Guidelines
- User is aware that DevAuthHandler uses a dummy user ID ("dev-user-id") and that exercises created via API in dev mode will be tied to that ID, not the seeded demo user. They accept this trade-off.